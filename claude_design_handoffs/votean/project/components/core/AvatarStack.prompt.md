Overlapping row of member avatars (-7px overlap, white ring). Shows a "+N" bubble past `max`, and optionally a dashed add button at the end.

```jsx
<AvatarStack members={topic.members} max={4} onAddClick={openShare} />
```
