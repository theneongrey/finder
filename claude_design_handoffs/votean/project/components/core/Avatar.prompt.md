Round initials avatar for a person. Pick bg/fg from the 6-color person palette tokens, assigned round-robin (never the same color logic as status/semantic colors).

```jsx
<Avatar initial="G" bg="var(--person-1-bg)" fg="var(--person-1-fg)" size="md" />
```
