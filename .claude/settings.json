{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "timeout": 120,
            "statusMessage": "Linting and formatting frontend files...",
            "command": "$j = $input | Out-String | ConvertFrom-Json; $f = $j.tool_input.file_path; if ($f -match 'app[/\\\\]finder[/\\\\]src') { Push-Location 'C:\\repos\\finder\\app\\finder'; npm run lint; $lintExit = $LASTEXITCODE; npx prettier --write 'src/**/*.{ts,html,css}'; Pop-Location; exit $lintExit }"
          }
        ]
      }
    ]
  }
}
