Large circular yes (heart, teal outline) / no (X, red-tinted) buttons flanking a plain-text "Überspringen" skip link — the core interaction of the date-poll voting screen.

```jsx
<VoteButtons onYes={vote1} onSkip={skip} onNo={vote0} />
```
