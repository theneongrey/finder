White rounded surface with a soft shadow — the base every content card in Votean sits on. Set `accentBorder` for the 4px teal left-border variant (question summaries, poll-option editor cards).

```jsx
<Card>{children}</Card>
<Card accentBorder padding={18}>{optionEditorFields}</Card>
```
